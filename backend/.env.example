# Server
PORT=3000
NODE_ENV=development

# Google Gemini AI (AyurBot + Symptom Checker)
GEMINI_API_KEY=your_gemini_api_key_here

# Firebase Admin SDK
# Paste the full JSON service account as a single line (no line breaks)
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"deergayu-9de41",...}

# Admin access (comma-separated emails)
ADMIN_EMAILS=yes.manujaya@gmail.com

# Email (optional — uses Ethereal test account if not set)
SMTP_USER=
SMTP_PASS=
