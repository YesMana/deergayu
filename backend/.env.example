# Copy to /home/dilspxws/api/.env on cPanel (never commit real passwords)
# Quotes are required when the password contains # @ etc.

SMTP_HOST=localhost
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=info@deergayu.com
SMTP_PASS="YOUR_MAILBOX_PASSWORD"
ADMIN_EMAIL=yes.manujaya@gmail.com
SUPPORT_EMAIL=info@deergayu.com

# Required for auth / data (already set on host — never commit real values):
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}
# FIREBASE_STORAGE_BUCKET=deergayu-9de41.firebasestorage.app
# GEMINI_API_KEY=
# PAYHERE_MERCHANT_ID= / PAYHERE_SECRET= / PAYHERE_SANDBOX=true
# RESEND_API_KEY= / RESEND_FROM= (if using Resend on Render)
# PUBLIC_API_URL= https://deergayu.com or Render URL
